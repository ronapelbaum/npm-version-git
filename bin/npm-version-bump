const npmVersionBump = require('../lib/npm-version-bump');

const args = process.argv.slice(2);
const getArg = (key) => (args.includes(key) && args[args.indexOf(key) + 1]) || null;

npmVersionBump(getArg('-rgx'), getArg('-def'));
